{
  "name": "practice-node-project",
  "version": "0.0.1",
  "description": "Node.js项目实战",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npm run clean && babel src -d target --watch",
    "compile": "npm run clean && babel src -d target",
    "clean": "rm -Rf target",
    "dev": "export NODE_ENV=dev && export DEBUG='my:*' && node target/server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/roninby/practice-node-project.git"
  },
  "author": "Ryan <roninby@yeah.net>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/roninby/practice-node-project/issues"
  },
  "homepage": "https://github.com/roninby/practice-node-project#readme",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015-node4": "^2.1.0",
    "babel-preset-stage-0": "^6.5.0"
  },
  "dependencies": {
    "project-core": "^0.1.3"
  }
}
